programa {
  funcao inicio() {
    inteiro velocidade
    escreva("digite velocidade")
    leia(velocidade)
    se(velocidade>80)
    escreva("passou do limite de velocidade")
    senao escreva("dentro do limite")
  }
}
